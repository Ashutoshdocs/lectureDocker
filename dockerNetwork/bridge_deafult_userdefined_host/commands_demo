#
ip address show
bridge link
docker exec containername ip addr


#list network names
docker network ls

#docker study your network
docker inspect networkName


#docker comamnd to show the networkiprange and containerips held in it:
docker network inspect YOUR_NETWORK_NAME --format 'Network: {{.Name}} (CIDR: {{(index .IPAM.Config 0).Subnet}}){{"\n"}}{{range .Containers}} - {{.Name}} -> {{.IPv4Address}}{{"\n"}}{{end}}'

#list containers in the network:
docker ps -a --filter "network=YOUR_NETWORK_NAME"

#cidr of the docker networks
docker network inspect $(docker network ls -q) --format '{{.Name}} -> {{range .IPAM.Config}}{{.Subnet}}{{end}}'



#docker network creation
docker network create networkName

#create container inside the network
docker run -itd --name c01 --network networkname nicolaka/netshoot
docker run -itd --name c02 --network networkname nicolaka/netshoot




